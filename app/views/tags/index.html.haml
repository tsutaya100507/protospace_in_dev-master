= render 'prototypes/top'
.proto_list_tag
  .row
    - @tags.each do |tag|
      = link_to tag_path(tag.id) do
        .proto_content_tag
          .thumbnail_tag
            .tags_title__btn
              = tag.title
            .tags_list__number
              = tag.prototypes.count
              %i project
